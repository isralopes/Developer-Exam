# Database Configuration
# Format: postgresql://username:password@host:port/database
# Note: Docker container uses port 5433 on host to avoid conflicts with local PostgreSQL
# DATABASE_URL=postgresql://dev_user:dev_password@localhost:5433/user_management

# Server Configuration
PORT=3001
NODE_ENV=development
